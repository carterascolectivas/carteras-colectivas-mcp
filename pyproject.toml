[project]
name = "carteras-colectivas-mcp"
version = "2.22.2"
description = "MCP bridge for Colombian mutual funds (FICs) — forwards requests to the Carteras Colectivas Lambda API"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "requests>=2.31.0",
]
